<mvc:View
    controllerName="ztdadmmotorista.controller.App"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">

    <Shell>
        <App
            id="app"
            height="100%"
            busy="{appView>/busy}"
            busyIndicatorDelay="{appView>/delay}"/>
    </Shell>

</mvc:View>